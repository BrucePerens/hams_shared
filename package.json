{
  "name": "hams-shared-lint-config",
  "private": true,
  "description": "ESLint configuration shared by hams_com and hams_open, run by tools/run_linters.py as a required (fail, not warn) CI gate.",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.0",
    "eslint-plugin-promise": "^7.3.0",
    "globals": "^17.11.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.67.0"
  }
}
